# 变体管理

每个变体必须是可以直接复制到聚宽运行的完整 `.py` 文件，并且只验证一个主要假设。

计划中的第一轮变体：

1. `allocator-redistribution.py`：修复行业和模型上限导致的被动现金。
2. `catalyst-filter.py`：增加基本面改善或价格确认。
3. `portfolio-risk-overlay.py`：增加组合级风险预算。

变体经过滚动验证前，不替换 `baseline.py`。
