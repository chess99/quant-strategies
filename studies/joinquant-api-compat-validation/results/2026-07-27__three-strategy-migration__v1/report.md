# 聚宽日频兼容层三策略验收 v1

本次运行的三类查询逻辑和点时检查实际通过，但归档器把 NumPy `bool_` 通过
`default=str` 写成字符串，并在生成本报告前抛出序列化异常。因此 v1 归档不作为正式
验收结论；已保留原始输出、运行源码和 manifest 作为失败实验，不修改覆盖。

修正后的完整验收新增为 `2026-07-27__three-strategy-migration__v2`。
