# 变体

本轮参数和模块开关全部由 `research.py` 按冻结协议生成，不把样本内冠军写成平台变体。
只有经过事前成功标准和平台校准的结构才会在未来迭代中新增独立 `.py` 变体。

2026-08-21 的可交易化 v3 已完成 136 次直接回测，但只通过 16 项事前门槛中的 9 项，故没有
新增 `tradability_v3.py` 变体，也不替换 `baseline.py`。归档内的 `source.py` 是固定池平台迁移
工作包，用于未来确有实盘需求时继续校准，不代表已通过验证的可运行变体。
