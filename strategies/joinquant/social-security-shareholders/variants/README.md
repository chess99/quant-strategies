# 变体

本目录中的文件均为完整、可独立运行的聚宽策略。

| 文件 | 与基线的唯一差异 |
|---|---|
| `top10.py` | `TARGET_COUNT = 10` |
| `top20.py` | `TARGET_COUNT = 20` |
| `top50.py` | `TARGET_COUNT = 50` |

其他逻辑必须与 `baseline.py` 保持一致；如果修改信号、过滤条件或组合方法，应新增描述假设的变体，而不是覆盖这些集中度对照。
