# 回测归档

每个目录都是不可变实验，至少保存：

- `manifest.json`：口径、成本、数据指纹、指标和限制；
- `report.md`：事实、推断与下一步；
- `source.py`：本次本地因子及撮合源码；
- `raw/`：净值、逐年收益、成交与逐月候选。

2026-07-26 的 `local-qlib-eastmoney-2019-2025-v1` 系列使用完全相同的数据与撮合，
只改变模型规则。不得以同名目录覆盖；修正错误时新增运行标识。

