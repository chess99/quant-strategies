# 指数择时首次复现源码入口补充归档

## 事实

- 本目录没有重新运行 MACD 或两份 RSRS 回测，不产生新的绩效结果。
- 原归档 `2026-07-27__local-qlib-comparison-v1` 已保存精确 `engine.py`、引擎 SHA-256、
  三份聚宽原策略路径及各自 SHA-256，但没有保存名为 `source.py` 的入口。
- 本目录的 `source.py` 会先验证原归档 `engine.py` 的 SHA-256 必须为
  `58c3c219ad1b0ac9078aa947a563cbcc4e9b01df0d02ed4995d8559e1ac2a4fc`，匹配后才执行。
- 三份原策略哈希同时复制到本附录 manifest，防止只证明本地引擎而遗漏比较对象。

## 判定

原归档保持不可变；本附录只补充可验证、可执行的标准 `source.py` 入口。所有指标、行情版本
和限制仍以原归档为准，本目录不是一次新的回测。
